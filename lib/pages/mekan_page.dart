import 'package:flutter/material.dart';

class MekanPage extends StatelessWidget {
  const MekanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MekanPage"),
    );
  }
}
